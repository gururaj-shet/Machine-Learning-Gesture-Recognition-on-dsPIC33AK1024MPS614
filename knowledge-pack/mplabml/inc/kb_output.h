/*******************************************************************************
  kb_output.h — compatibility shim.  Real MPLAB ML packs sometimes split the
  output-format helpers into a separate header.  For this hand-crafted pack
  everything is declared in kb.h, so we just re-export it.
*******************************************************************************/
#ifndef KB_OUTPUT_H
#define KB_OUTPUT_H

#include "kb.h"

#endif /* KB_OUTPUT_H */
